<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> CD</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css" >
</head>
<body class="contenedorBar">

  <%- include('./partials/navbar.ejs') %>
  <%- include('./partials/message.ejs') %>
  
 <% results.forEach((productos) => { %>
 
      <div class=" inicio">
        <div class="row">
          <div class="col-md-40 ">
        <div class="card" id="efectocarrito"  >
          <form action="/agregar" method="post" class="form-control" >
            <input type="text" class="form-control " name="imagen" id="imagen" value="<%= productos.imagen %>"  hidden> <img style="width: 150; height: 150px;" src="/imagen/<%=productos.imagen %>" class="rounded mx-auto d-block ">
            <input type="text" class="form-control-plaintext" name="producto" id="producto" value="<%= productos.producto %>" readonly="readonly" hidden>
            <h4> <input type="text" class="form-control-plaintext" name="nombre" id="nombre" value="<%= productos.nombre %>" readonly="readonly"> </h4>
            <input type="text" class="form-control-plaintext" name="description" id="description" value="<%= productos.description %>" hidden>
            <input type="number" class="form-control-plaintext" name="cantidad" id="cantidad" min="1" max="<%= productos.cantidad %>" value="0">
           <h4> <input type="number" class="form-control-plaintext" name="precio" id="precio" value="<%= productos.precio %>" readonly="readonly"></h4>
           <% if (productos.cantidad == 0){ %>
            Lo sentimos no tenemos ese producto
             <% } else {%>
              <a href="information/<%= productos.id %> " class="btn btn-primary"> Informacion  </a>
   
               
                  <button type="submit" class="btn btn-primary"> Agregar al carrito </button>
                 <% } %>
            
          </form>
        </div>
          </div>
        </div>
        </div>
    <% }) %>
</body>
<%- include('./partials/footer.ejs') %>
</html>